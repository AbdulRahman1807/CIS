# The entire pipeline (connector, collector, allowlist, rules, prioritizer,
# report, cli, and Web UI) uses the Python 3 standard library only.
# No external packages are required to run the core agent or serve the UI.
