# connector.py / collector.py / allowlist.py use only the Python 3 stdlib
# (subprocess, json, pathlib) — nothing to install for Person A's part.
#
# Person B / Person C: add whatever rules.py / prioritizer.py / report.py /
# cli.py actually end up needing here as you go (e.g. an LLM SDK only if the
# --llm stretch in WORKPLAN.md §4 gets built and organisers permit it).
